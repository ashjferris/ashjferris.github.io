{
  "name": "ash-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "python3 scripts/build.py",
    "clean": "rm -rf dist",
    "serve": "python3 -m http.server 8080"
  }
}

